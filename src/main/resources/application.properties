spring.application.name=otp-service

# --- PostgreSQL ---
spring.datasource.url=jdbc:postgresql://localhost:5432/otp_db
spring.datasource.username=valeriali
spring.datasource.password=

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Server ---
server.port=8080

# --- Logging (Logback) ---
logging.level.org.springframework=INFO
logging.level.com.leralee.otpservice=DEBUG

jwt.secret=supersecretkeysupersecretkey123456
jwt.expiration=1000

telegram.bot.username=otp_helper_bot